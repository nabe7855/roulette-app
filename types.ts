// このファイルは、アプリケーションで使うデータの「型」を定義しています。
// 「型」とは、データの構造や種類をあらかじめ決めておく設計図のようなものです。
// これにより、予期しないデータが使われるのを防ぎ、コードの安全性を高めます。

// ルーレットの各セクション（扇形の部分）が持つ情報を定義しています。
export interface Segment {
  color: string; // セクションの色（例: '#ef4444'）
  label: string; // セクションに表示されるテキスト（例: 'おしゃべり番長'）
}
